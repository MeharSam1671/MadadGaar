import 'package:flutter/cupertino.dart';

class FullTextScreen extends StatelessWidget {
  const FullTextScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
